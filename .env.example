# All optional. make seed works with none of these set.

# GitHub API token — raises rate limit for live ingest (fixtures used offline)
GITHUB_TOKEN=

# Anthropic API key — slot-fill for ambiguous questions and compare synthesis only
ANTHROPIC_API_KEY=

# Set to 1 to fetch live data during make ingest (default: fixtures only)
INGEST_LIVE=0
